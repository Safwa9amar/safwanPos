// This is your main schema file. It imports all other schema files.

// -----------------
// BASE
// -----------------

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "sqlite"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

// -----------------
// MODELS
// -----------------

// Import all your model files here

@@include("schemas/user.prisma")
@@include("schemas/inventory.prisma")
@@include("schemas/sales.prisma")
@@include("schemas/crm.prisma")
@@include("schemas/purchasing.prisma")
@@include("schemas/operations.prisma")
